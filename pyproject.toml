[project]
name = "python-algorithms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click",
    "ipython",
    "rich"
]

[project.scripts]
ralgo = "clis.huffman:main"

[tool.uv]
package = true
